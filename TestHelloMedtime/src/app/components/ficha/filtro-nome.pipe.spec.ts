import { FiltroNomePipe } from './filtro-nome.pipe';

describe('FiltroNomePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNomePipe();
    expect(pipe).toBeTruthy();
  });
});
